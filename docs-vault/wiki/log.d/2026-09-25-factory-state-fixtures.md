- 2026-09-25 — Shared fixture `factoryStatesAndIngredients` (`packages/shared/fixtures/factory.ts`), fixtures only and no contract change, for the frontend's Ingredients and State colours. Nine machines with invented ids and numbers: every backend-derived state (producing, idle, backedUp, `underfed`, paused, unpowered), one machine with an unknown state string (`overclocking-ish`, to test the neutral fallback), one with no `state`, one with neither `ingredients` nor `state` (an older backend, ADR-0007), one with an empty `ingredients` array (no recipe), and a Packager with a solid (`items/min`) and a fluid (`m3/min`) input. `stateCounts` and `backedUpCount` match the machines. It is matched to `FactoryResponseSchema` by the existing name-prefix drift test, and `factoryState.test.ts` pins what it covers (all six states, the unknown and missing state, solid and fluid inputs, empty and absent ingredients, honest counts, no `starved`, no real host or token). `clockSpeedPercent` is not in it yet: the clock speed contract PR adds it to a couple of machines (100, 50, 160).
