- 2026-09-26 — Frontend, ADR-0027 PR 9a: the Alerts page, read-only. A bell in the header (the owner's choice over a sixth tab, so the five tabs still fit at 390 px) opens `/app/alerts`, and its badge counts the selected server's firing alerts. The badge reads `alerts/status` once a minute (never at the page's rate), and hides on any error, with its own crash boundary. The page shows whether delivery is on ("Delivery is off (shadow week)" otherwise), the mute, what's firing, the Discord destination by its last 4 characters (with why it's off), and the alert log paged by `nextBefore`. The query layer gains the alerts queries and `useAlertWrites` (every write, for 9c's owner/admin controls; the webhook URL is never cached), and `apiGetQuery` leaves an undefined optional parameter out of the URL. The demo has its own alerts: presets plus one production target, two firing and three resolved alerts, a pretend webhook and in-memory writes; "Send test" never contacts Discord. New scenarios `alerts`, `alerts-webhook-gone` and `alerts-empty`; every page's screenshot gains the bell.
