- 2026-09-26 — ADR-0027 PR 9c, the Alerts page's owner/admin controls. `canEditAlerts(role)` (alertText.ts) is true for owner and admin only; an absent or unknown role reads the page as before, and the backend's 403 shows like any error. Under "Now", a mute until a chosen local time (at most 7 days ahead, checked before sending; the backend's mute_invalid shows its message) and Unmute now. Under "Discord", set or replace the webhook in a password field that is sent only through `useAlertWrites().saveWebhook` (never a mutation's variables) and cleared once saved, plus Send test (every SendTestResponse code in words via `sendTestText`; the demo says "Sent (demo)"), Turn off/on and Remove (inline confirm). A new "Rules" section mounts the 9b editor on createRule/updateRule/deleteRule, with today's factory items to pick a target from. The rules editor now uses alertText's kind and severity names. The status and destination panels take their controls as children. The demo visitor is the server's owner. Scenarios gain the nine alert write routes and an `alerts-owner` state.
