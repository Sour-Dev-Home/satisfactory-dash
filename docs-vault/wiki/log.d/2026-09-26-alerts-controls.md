- 2026-09-26 — ADR-0027 PR 9c, Settings → Alerts (the owner's redesign: no full Alerts page; the header's alert dropdown links to `/app/settings#alerts`). A new section (`frontend/src/alerts/settings/AlertSettings.tsx`, mounted after the auto-pause settings) with three parts that each load and fail on their own: Status (delivery off, the mute, what's firing), Discord and Rules. Every member reads it; `canEditAlerts(role)` (alertText.ts) gives owners and admins the controls, an absent or unknown role reads, and the backend's 403 shows like any error. Controls: a mute until a chosen local time (at most 7 days ahead, checked before sending; mute_invalid shows its message) and Unmute now; the Discord webhook in a password field sent only through `useAlertWrites().saveWebhook` (never a mutation's variables) and cleared once saved, with Send test (every SendTestResponse code in words via `sendTestText`; the demo says "Sent (demo)"), Turn off/on and Remove behind an inline confirm; the 9b rules editor on createRule/updateRule/deleteRule with today's factory items. The rules editor now uses alertText's kind and severity names. The demo visitor is the server's owner. Scenarios gain the nine alert write routes and `alerts-owner` / `alerts-viewer` states on the Settings page.
