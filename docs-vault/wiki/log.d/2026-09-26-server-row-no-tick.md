- 2026-09-26 — Frontend (#172): the server tick lives only in the Health card (the owner's calls on #170). The Overview's Server row no longer says "Server tick is slow"; a slow tick still makes the overall health a warning through `tickState` in `frontend/src/overview/health.ts`, an input with no row, and joins the dismissal key so a hidden warning comes back when the tick turns slow. Because no row names it, the owner chose option A: each warning carries a short `cause`, and when a slow tick is among the warnings the Health headline names the causes, tick first ("Server tick is slow · Factory backed up"); otherwise the headline is unchanged. A slow-and-paused server now says "Paused" in its row.
