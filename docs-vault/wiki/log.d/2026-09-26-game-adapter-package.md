- 2026-09-26 — ADR-0031 PR 2, no behaviour change: the game-server adapter is extracted from `backend/src/modules/gameserver/` into the workspace package `packages/game-adapter` (`@satisfactory-dash/game-adapter`), so the edge agent can reuse it. Moved (as renames): `domain`, `rawSchemas`, `rawTypes`, `vanillaApiClient` (with the transport), `frmApiClient`, `satisfactoryServerAdapter`, `serverOptionsAdapter`, `connectionTest`, their tests and the captured and raw fixtures (now `@satisfactory-dash/game-adapter/fixtures`, tests only). New in the package: `errors.ts` (`UpstreamError` and `RequestFailureKind`, moved from the backend's `platform/errors.ts`, which now re-exports the very same class so `instanceof` still works), `connection.ts` (the `SatisfactoryServerConfig` type and `createSatisfactoryServerConfig`, which the edge agent needs), `factories.ts` (`createGameServerConnection`, `createServerOptionsPort`) and a boundary test that fails if the package imports anything but its own files, node built-ins, zod and the bare shared package (no Express, no backend path). Stays in the backend: `connectionConfig.ts` (environment parsing), `serversFile.ts`, `captureSeries.ts`, and `modules/gameserver/index.ts`, now a thin facade with an EXPLICIT export list (exactly what the module exposed before, so `ServerOptionsAdapter` stays unreachable from it and GetServerOptions stays behind the allowlisted port, ADR-0012; a test pins the list), so other modules' imports and the module edges are unchanged. Two tests that exercised the adapter through `ProductionService` moved to `telemetry/services`. The backend's architecture test gains rule 7 (only `modules/gameserver/` and `platform/errors.ts` import the package, and only its bare name, never `/fixtures`), the package is wired into the root scripts, the Dockerfile and `workspace.dsl`, and the README moved with the code. Proof: the backend and package suites together run the same tests plus the boundary and architecture additions; `git diff --stat` is mostly renames.
