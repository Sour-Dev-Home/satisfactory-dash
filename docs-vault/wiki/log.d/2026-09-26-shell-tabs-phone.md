- 2026-09-26 — Frontend: the main tab bar fits at 390 px. Below the `sm` breakpoint the tabs use `px-2.5` and the nav has no gap, so Overview, Power, Factory, Map and Settings all show (the last was clipped to "Settin…" since the nav started scrolling in #169). Each tab stays 44 px tall; an operator's sixth tab (Servers) still scrolls inside the nav, and 360 px still scrolls. Desktop is unchanged. Mobile screenshot baselines regenerated, since every page shows the tab bar.
