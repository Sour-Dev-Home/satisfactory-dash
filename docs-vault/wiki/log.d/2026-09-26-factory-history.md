- 2026-09-26 — ADR-0027 PR 8b, production history on the Factory page. Above the table, "Since yesterday" compares each item's latest complete hour (the 7d range's hourly buckets, 2 minutes after the hour closes) with the same hour 24 h earlier, in text: the biggest drop as the headline, then up to 3 notable drops and rises (`NOTABLE_CHANGE` in `frontend/src/factory/sinceYesterday.ts`: 10% and 1 per minute, both; a default the owner can change), and how many state changes the last 24 h held ("500+" when the backend held more back). Without a day of history it says so instead of showing an empty list. Below the table, "Production history": the Power page's range picker (1 h … 1 y, per visit), an item picker in the backend's order, a text summary, a lazy uPlot chart of the bucket average against capacity (a break where nothing was recorded) and a readings table. History carries only class names, so names and units come from the live factory (`itemLabels.ts`); an item not in today's factory shows its class name made readable, with "per min". New `queries.historyItems` / `historyTransitions`, `formatPerMinute` / `formatAmount` / `unitLabel` in `format.ts`, default MSW handlers and scenario routes for both history endpoints. The demo world records its own items (the power history's range table, one copy) with a paused gap two days back and a visible dip (the Rotor line backed up 5 h ago, the Screw line short of rods for 6), plus state changes; every range validates against the schema, and all six generate in about 45 ms.
