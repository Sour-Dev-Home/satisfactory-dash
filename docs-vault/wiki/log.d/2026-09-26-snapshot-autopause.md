- 2026-09-26 — Contract (ADR-0031): `SnapshotRequestSchema` in `packages/shared/src/agent.ts` gains an optional `settings: { autoPause: boolean }` (a strict object, optional as a whole), so the agent can report the game's auto-pause setting with each snapshot. An agent that omits it changes nothing: the backend's ingest already accepts it and ignores it. The `agentSnapshotRequestFull` fixture carries it and `packages/shared/test/agent.test.ts` covers the shape. Serving it from the settings route, overridden by a pending or confirmed `set_auto_pause` command, follows in a separate PR once the commands PR (#248) is on main.
