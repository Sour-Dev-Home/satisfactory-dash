- 2026-09-26 — Frontend design-token audit (the owner's token rule). The 38 raw values in `frontend/src` (touch targets, button padding, chart and map heights, the overview rail width, the Factory table breakpoint, the account menu's z-index, and the banner and error-notice colours in `index.css`) move to named tokens in the `@theme` block of `src/index.css`, with no visual change: the screenshot baselines are untouched. New `frontend/scripts/check-design-tokens.mjs` (rules in `design-tokens.mjs`, unit-tested) runs in `npm run lint` and fails on raw colours, bracketed lengths, raw z-index and raw motion values outside `@theme`. The escape hatch is `design-token-allow: <reason>` on or above the line; the script's `ALLOWED_EXCEPTIONS` count must match the hatches in `src/` (0 today). One edge fixed on the way: at exactly 600 px wide, the Factory table used to show both the stacked label and the dash; the shared breakpoint token now puts them on the same side.
