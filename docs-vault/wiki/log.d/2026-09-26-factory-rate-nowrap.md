- 2026-09-26 — Factory table (#59, from the #48 UI review): each output's "current / max unit" is one `whitespace-nowrap` group in `FactoryPanel.tsx`, so a narrow phone only wraps before the "(%)" and never splits the numbers from the unit. The text and the screen-reader string are unchanged; a new test checks the group. The optional "unit unknown" cue for the "per min" fallback is left to the table rework in #184.
