- 2026-09-25 — ADR-0027 amendment 2, PR 2b-1: the machine state `starved` is renamed `underfed` and the rule is the owner's: the averaged output percent below 95 (`UNDERFED_BELOW_PERCENT`, replacing `STARVED_BELOW_PERCENT` = 5; provisional until the capture session replay, 2b-2). The percent is relative to the SET clock (FRM's `MaxProd` already includes the clock speed), so a fully fed underclocked or overclocked machine reads about 100 and stays `producing`. Precedence is unchanged (paused > unpowered > idle > backedUp > underfed > producing), so a backed-up machine with a low percent stays `backedUp`; the `missingInput` hint (lowest `ConsPercent`) stays; `bestOutputPercent` is still the maximum across outputs. The amendment text is appended byte-identical to `decisions/0027-history-and-alerts.md`. The shared `factory.ts` descriptions for `state` and `stateCounts` say `underfed` (still a plain string, no schema change); the rename covers the classifier, its tests, the production-state and history-poller tests, the shared test and `machine-states.md`. The replay golden changed for exactly one machine, hand-checked against the capture: Assembler ...2147397136 (9.4 percent, output 98 of 100) goes from `producing` to `underfed`; every other captured machine keeps its state (the 160 percent-clock stator at 24.7 percent has a full output and stays `backedUp`; the refineries read 100 or 0 percent). That machine has full input buffers, so it is output-throttled rather than input-starved: a known false "underfed" the capture session should measure (recorded in `machine-states.md`). No alert rule is added here; "Stopped machines" and "Newly underfed" are PR 5 presets.
