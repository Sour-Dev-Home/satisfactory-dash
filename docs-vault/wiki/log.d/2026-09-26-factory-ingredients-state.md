- 2026-09-26 — Factory view, #184 (ADR-0027): each machine's inputs, state and clock speed. An Inputs column mirrors Outputs (the same unit resolution, "per min" fallback and one plain line each); `ingredients: []` shows a dash, and the column is left out when no machine has the field (an older backend). The backend's `state` is a label in the new State column (was Notes), its colour from `frontend/src/factory/machineState.ts` (producing ok, idle muted, backed up and underfed warn, paused info, unpowered bad), and the text carries the meaning; an unknown or missing state shows nothing, and the Backed up / Paused flags stay as labels when the state doesn't already say them. The clock shows under the recipe as "Clock 160%" only when it isn't 100% (the owner's call, 2026-09-25: never combined with the state label). The summary says "1 machine". On a phone the stacked lists are named Inputs and Outputs. The demo world has its own ingredients, states (one underfed, one backed up) and one 150% smelter. The optional "unit unknown" cue stays out (a muted label alone doesn't explain itself).
