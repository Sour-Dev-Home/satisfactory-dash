- 2026-09-26 — CI (ADR-0032 step 4): new scheduled workflow `.github/workflows/latency-probe.yml`. Twice an hour (and on demand) it requests `https://api.satis-manager.com/api/health/ready` once with curl, writes the status, DNS, TCP, TLS, first-byte and total times to the job summary (no response body), and fails when the status is not 200, the request fails, or the total is over 2 seconds. It has `permissions: {}`, no checkout and no secrets, and reads only the public readiness endpoint.
