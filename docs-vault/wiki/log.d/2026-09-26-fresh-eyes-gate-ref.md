- 2026-09-26 — Fix: the merge queue's fresh-eyes gate failed closed on its first real run (PR 207, run 36214916402), so the queue dropped the entry. `merge_group.head_ref` is the FULL ref (`refs/heads/gh-readonly-queue/main/pr-207-<sha>`), while the script only accepted the bare branch name, so it said "cannot tell which PR this queue entry is for". `parsePrNumber` in `scripts/fresh-eyes-gate.mjs` now strips exactly ONE leading `refs/heads/` and then matches the same strict pattern (`gh-readonly-queue/main/pr-<N>-<40 hex>`, no leading zero, at most 9 digits), still failing closed on anything else (a doubled prefix, `refs/tags/`, another base branch, a non-numeric PR, trailing junk, whitespace, a short sha). Tests use the real value from that run (and the bare form), plus the negative cases, and a `runGate` test with the real ref. It failed closed, so nothing unreviewed could have merged; the queue rule was removed temporarily (the ADR-0033 rollback path) and this PR merged the normal way, because the gate checks its script out from the queue's base commit, so a fix cannot pass through the broken gate. ADR-0033 amendment 2 is appended byte-identical to `decisions/0033-delivery-workflow.md`.
