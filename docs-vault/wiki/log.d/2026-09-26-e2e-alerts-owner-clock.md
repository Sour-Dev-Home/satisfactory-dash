- 2026-09-26 — Test only: the `alerts-owner` screenshot (Settings → Alerts) was flaky and knocked a PR out of the merge queue. Its mute field defaults to an hour from now on the real clock, so the rendered time, and with it the page height, changed between runs. The e2e state cases take an optional fixed `clock`, applied with `page.clock.setFixedTime` before the page loads; `alerts-owner` and `alerts-viewer` use the fixtures' day. No app code changes.
