- 2026-09-26 — ADR-0032 (latency and UX budgets), accepted by the owner, added byte-identical to the architect's draft as `decisions/0032-latency-and-ux-budgets.md`, with its row in the decisions README. It sets five steps: split the server time (`Server-Timing`, `npm run latency-report`), turn "feel" into failing Playwright tests (tab switching without staged pop-in first), show data age, a scheduled public probe of `/api/health/ready`, and a human check on a throttled phone. Docs only; the build plan has the server-time split next.
