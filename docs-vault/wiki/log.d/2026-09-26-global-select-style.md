- 2026-09-26 — `select` is styled with the inputs in `frontend/src/index.css` (the base layer's `input` rule now covers `select` too: a 44 px target, the line border, the 8 px radius, the canvas background), and an invalid select (`aria-invalid="true"`) gets the bad border. Found by the ui-reviewer on PR 9b, whose selects were 19 px tall with no border; the Factory history's item picker gets the same fix with no change of its own. The rules editor's local select classes (`fieldAttrs.ts`, added in #229 for exactly this) are gone.
