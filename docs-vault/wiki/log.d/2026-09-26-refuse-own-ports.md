- 2026-09-26 — Backend (#195): server management refuses a loopback game server that uses this backend's own listen port (`PORT`) or the database's port (the one `DATABASE_URL` names, 5432 by default), in the candidate test (`POST /api/servers/test-connection`), create, edit (checked against what would be saved: a patched port, or the stored one when only the host changes) and the saved test (a row stored before the rule is not tested either). Before, an operator could aim a test at `127.0.0.1:<backend or Postgres port>` and make the backend send the entered token to and map its own internals. The answer is `address_not_allowed` (422) with a message that names no port, address or token. Only loopback addresses are checked (another machine's port number is not this backend). `ServerManagementDeps.forbiddenPorts` is supplied by the composition root; `databasePortOf(url)` reads the port without ever exposing the URL (which holds the password). `import-servers` (an offline operator CLI) is unchanged. Runbook `servers.md` updated.
