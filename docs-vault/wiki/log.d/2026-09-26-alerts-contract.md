- 2026-09-26 — ADR-0027 PR 7a, the alerts contract in `packages/shared` (schemas, endpoints, fixtures, contract tests; NO routes, so nothing is served yet and the frontend can build against the fixtures). New `src/alerts.ts`: rules (`AlertRule`, list, create, update, delete), the Discord destination (only its last 4 characters and state come back; the webhook URL exists ONLY in the PUT request body), the alert log (`AlertEvent`, events query `limit` 1 to 100 default 50 and `before`, `nextBefore` cursor, ids as strings), status (`deliveryEnabled`, `mutedUntil`, `firing`) and mute. Thirteen endpoints under `/api/servers/:serverId/alerts/...` (every GET for any member, every other method owner/admin only, the existing rule). Deploy skew: RESPONSE fields that name kinds, severities, transitions, reasons and codes are plain strings with the known values in `.describe()`; REQUESTS are strict objects and enums. Only `production_below_target` can be created (a discriminated union on `kind`, defaults warning, enabled, 600/300/3600 s); a PATCH takes at least one of enabled, severity, the three durations and `params` (a plain record so the backend can answer `rule_item_immutable` for an `item`; the UI builds it from `ProductionBelowTargetUpdateParamsSchema`); bounds match the DB CHECKs. Typed per-kind params (`StoppedMachinesParamsSchema` and `ServerUnreachableParamsSchema` too, since the presets carry them) and typed event summaries (power outage, server unreachable, stopped machines with both reasons and `newMachines`, production below target with an optional average). Eight new error codes (`rule_not_found` 404, `rule_item_immutable` 422, `preset_disable_only` 409, `rule_kind_not_creatable` 422, `destination_not_configured` 404, `webhook_invalid` 422, `delivery_off` 409, `mute_invalid` 422) and an optional `reason` on the error envelope (a stable sub-code such as `host_not_allowed`, never user input). Both exhaustive code maps are updated: the backend's HTTP status map and the frontend's error-kind map. The backend's generated authorization tests exempt exactly the thirteen alerts endpoints until PR 7b mounts the routes (a guard test pins the exemption at 13). Fixtures: rules (the three presets and a disabled production rule), destinations (configured, none, webhook gone), event pages (all four transitions, both stopped-machine reasons, a production event, a cursor and a last page), status (shadow week, muted, one firing; and quiet), mute, the test-send results, and one error fixture per new code; a contract test pins bounds, strictness, deploy-skew tolerance, that no response fixture can carry the webhook URL, and the endpoint table.
