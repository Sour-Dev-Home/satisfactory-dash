- 2026-09-25 — ADR-0033 (the delivery workflow, accepted by the owner) added as `decisions/0033-delivery-workflow.md` (the architect's draft, except that the board fields say "owner" instead of the owner's name, because this repo is public and CI's PII scan rejects it; a sentence notes that the private board labels that column with the name), with amendment 1 appended byte-identical to its draft, and its row in the decisions index. Three parts: per-PR log fragments (delivered by the log fragments PR, so this entry is itself a fragment), a merge queue for `main` with a `fresh-eyes-gate` workflow that carries the reviewed status onto the queue's commit (the `merge_group` triggers and the gate come in the next PR, before the coordinator flips the ruleset), and one task board.
