- 2026-09-25 — ADR-0033 (the delivery workflow, accepted by the owner) added verbatim as `decisions/0033-delivery-workflow.md`, with its row in the decisions index. Three parts: per-PR log fragments (delivered by the log fragments PR, so this entry is itself a fragment), a merge queue for `main` with a `fresh-eyes-gate` workflow that carries the reviewed status onto the queue's commit (the `merge_group` triggers and the gate come in the next PR, before the coordinator flips the ruleset), and one task board.
