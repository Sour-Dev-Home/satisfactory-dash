- 2026-09-25 — ADR-0030 PR 7, the combined security review of phase 1 (#161-#167, #169, #171; a fresh security-reviewer, static read of the whole diff). Result: nothing exploitable under `LAN_ALLOWED=false` with operator-only access, no High or Critical findings, `npm audit` clean. Fixed here: (L1) a malformed request body no longer puts body-parser's message, which quotes a snippet of the body and so possibly part of a token, into the log or a development response (the detail is now the error type and status only); and a soft delete now also removes the server's ADR-0027 history (samples, rollups, transitions), because a revived public id reuses the same internal id and would otherwise inherit the old server's history (DB test added, CI only). Recommended to the architect, NOT changed here: (L2) the operator's test-connection can probe any loopback port (a coarse open/closed oracle; suggest refusing the backend's own and the database port); (L3) the env-configured path classifies `*.localhost` names and 169.254/16 as private by name only (existing tests pin `*.localhost` as loopback, so this is a design decision); a size cap on game-server response bodies; printable-only display names (bidi spoofing); and, when `LAN_ALLOWED` flips, `createSatisfactoryServerConfig` hard-codes accepting a self-signed certificate, which the pinning change must replace. Confirmed sound: the address guard forms and the pinned-IP connect, AES-GCM (random nonce, AAD binding, key validation), operator-only enforcement and route order, the advisory lock and cap, tokens write-only in the UI, backups holding only ciphertext.
