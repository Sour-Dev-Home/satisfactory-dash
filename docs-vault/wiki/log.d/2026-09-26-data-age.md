- 2026-09-26 — Frontend, ADR-0032 step 3: the Status, Power and Factory views say how old their numbers are. "Updated 8 s ago" replaces "As of <time>" and counts up each second (`DataAge`, `dataAgeText.ts`, `lib/useNow.ts`); the exact time stays on the `<time>` element. Past twice the view's poll interval it turns amber and adds "newer data is overdue". It isn't a live region, since the stale banner already announces a lost connection. The e2e state tests now freeze the clock for every state (8 s after the live fixtures' `observedAt`, unless a case sets its own), so time-based text can't make a screenshot flaky; every baseline showing those views changes once.
