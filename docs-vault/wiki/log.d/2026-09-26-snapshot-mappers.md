- 2026-09-26 — Refactor, no behaviour change (ADR-0031 refactor A, architect ruling): the pure shape mapping from the game-adapter's domain types to the contract's parts moved from the backend's telemetry services into `packages/game-adapter/src/snapshot/` (`mapStatus`, `readPlayers`/`mapPlayers`, `mapPowerCircuit`, `mapFactoryBuilding`, `isBackedUp`), so the edge agent can produce exactly what the backend's services produce. The mappers emit the agent-input shapes from #255. The backend keeps the classification RULES: `classifyPowerCircuit` moved to `telemetry/services/classifyPower.ts` (re-exported from `powerService.ts`), and `agentDerive.ts` became `snapshotDerive.ts` (`derivePower`, `deriveFactory`), now applied to BOTH a polled server (mapper, then classify; each machine's fuse is the one FRM sent on it) and an edge agent at ingest (fuse joined from the power circuits). The four telemetry services are thin: mapper, then classify. The gameserver facade exports the mappers. Every existing production, power, status and players test passes unchanged; new package tests cover the mappers.
