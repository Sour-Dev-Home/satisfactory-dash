- 2026-09-26 — Backend (ADR-0031): `GET /settings` of an agent server now serves the auto-pause value the agent reports in `settings.autoPause` (contract in #250). `LatestSnapshotStore` keeps the latest reading (a late snapshot never replaces a newer one; an unreachable snapshot keeps it) and `telemetry.agentAutoPause()` exposes it with a staleness flag (three status intervals); the composition root passes it to `createAgentSettingsServices`, and `CommandsService.readAutoPause(serverId, reported?)` picks the value being applied, else the newer of the last confirmed change and the reading, serving the reading's own `observedAt`/`stale` through `observed()`. With no reading it behaves as before (confirmed change or `upstream_unreachable`). No new module edge. Runbook `agents.md` updated.
