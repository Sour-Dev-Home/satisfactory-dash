- 2026-09-26 — ADR-0035 cutover day: the nightly backup now uploads to Backblaze B2 (the owner ran the manual backup, the restore rehearsal from B2 and the nightly task, all passed). The privacy page now lists BOTH providers, because the older copies stay in S3 read-only for up to 37 days: a Backblaze item ("stores our encrypted backups") is added and the AWS item now says it still holds the older backups, adds nothing new and removes them within 37 days; the retention line reads "Backblaze stores encrypted backups for up to 37 days, so deleted data can survive in backups for up to 37 days", and the older AWS copies are removed within 37 days too (the owner approved the Backblaze wording; the region is unnamed). The privacy outline matches, and `runbooks/backups.md` gains the rehearsal record (2026-09-26: `pg_restore` exit 0, users 1/1, servers 1/1, members 1/1, connections 1/1, audit events 10/10, newest migration `1790812800000_alert_production_kind`) and the date the S3 bucket and the `satis-backup` IAM user are deleted (on or after 2026-11-02), when a second tiny PR removes the AWS row. No bucket name or endpoint is in any committed file. Docs and static page text only; no code.
