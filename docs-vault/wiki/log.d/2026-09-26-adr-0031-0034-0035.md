- 2026-09-26 — Docs only: three accepted decisions added to `decisions/`, each byte-identical to the architect's approved draft. ADR-0031, the edge agent (the game PC connects out; the backend never reaches in; "agent offline" is a default preset, on after the shadow week). ADR-0034, the backend and database on AWS, time-boxed on the account's credits, with portability requirements, an exit trigger and a rehearsed exit. ADR-0035, nightly backups move off AWS to Backblaze B2. `decisions/README.md` gains the three rows; ADR-0030's row already pointed at ADR-0031. No code, config or behaviour changes.
