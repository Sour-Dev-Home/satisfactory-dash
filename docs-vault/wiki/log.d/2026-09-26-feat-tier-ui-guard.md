- 2026-09-26 — ADR-0033 amendment 3 (review tiers, design tokens in CI, the test-hunter in parallel) appended byte-identical, with the `tier:ui` guard it describes: `scripts/tier-check.mjs` runs in the `verify` job on pull requests and, only for a PR labelled `tier:ui`, fails when a changed file (a rename's old path included) is outside `frontend/src/**/*.{ts,tsx,css}` (minus `api/`, `auth/`, `demo/handlers.ts`, `test/browser.ts`), `frontend/e2e/**` and `docs-vault/wiki/log.d/**`, or when a line added under `frontend/src` contains one of the fourteen data-flow patterns; a source file whose patch the API omits fails closed. The token lint is not in this change (it lands with the token-audit PR). `runbooks/backups.md` gains the B2 upload-key caveat (the web UI's "Write Only" may include delete; the 30-day governance Object Lock and the age key protect the copies) and the record of the `verify-secrets` check against a scratch restore of the newest B2 backup (1 stored connection opened, 0 unreadable).
