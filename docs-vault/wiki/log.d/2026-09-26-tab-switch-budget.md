- 2026-09-26 — ADR-0032 step 2, the first UX budget: `frontend/e2e/tab-switch.spec.ts` switches Power → Factory → Overview → Power with every API endpoint answering after a different delay (100-600 ms) and fails any switch above CLS 0.02, with motion on and off, at 1440 and 390 px. Shifts right after the click count too (the browser's CLS skips them as "recent input", which is exactly when the staged pop-in happens); each shift's moved elements are printed to the CI log. Measured before the fix: 0 on every switch except Factory on mobile with motion off, 0.078, the machine table jumping 95 px when "Since yesterday" landed above it (the page fade had hidden it with motion on). Fix: `FactoryView` reveals once, after the factory and both since-yesterday reads settle, then stays shown for that server (a failed read goes back to pending when "Since yesterday" retries it on mount, which would otherwise loop). No `staleTime` or prefetch was needed to meet the budget; prefetching the other tabs when idle stays an option for speed.
