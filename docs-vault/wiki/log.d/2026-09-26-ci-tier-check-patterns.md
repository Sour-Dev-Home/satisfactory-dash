- 2026-09-26 — The `tier:ui` guard (ADR-0033 amendment 3) tightened before anyone uses the label, on the architect's ruling: the data-flow pattern also matches `XMLHttpRequest`, `WebSocket`, `EventSource` and `sendBeacon`; `fetch` is a regex (`fetch\s*\(`, so `fetch (` and `fetch<tab>(` count); and an added line that names `@tanstack/react-query`, or is an import (static, re-export, dynamic, `require`) from a path with an `/api` or `/auth` segment, is data flow. `scripts/tier-check.mjs` now documents the accepted limits (a split or concatenated token, a different case, a symlink added as a `.ts` file): the guard stops an honest misclassification, not a malicious author. A test for each pattern, and for look-alike paths that must pass.
