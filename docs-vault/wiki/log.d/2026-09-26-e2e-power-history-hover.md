- 2026-09-26 — e2e flake: the mobile `power-history-24h` screenshot failed on #255 (no frontend change) and passed on re-run. The diff was the Account button's hover border (`button:hover` sets `--color-muted`), not the chart or its time axis: clicking "24 h" can scroll the page, and when it scrolls back the pointer rests over Account. The case now moves the pointer to the empty top-left gutter after the click. The stored-history window comes from the fixtures, not the browser clock, so no fixed clock was needed; `main` has no 7 d or Factory-history screenshot state to check.
