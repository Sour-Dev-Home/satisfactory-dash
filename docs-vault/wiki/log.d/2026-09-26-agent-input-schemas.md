- 2026-09-26 — Contract (ADR-0031, architect ruling): an edge agent's snapshot parts for power and factory are now AGENT-INPUT schemas, the response data schemas with every backend-derived field omitted: `AgentPowerSchema` (circuits without `status`, no `hasOutage`) and `AgentFactorySchema` (buildings without `state`, no `stateCounts`/`backedUpCount`, rates without `unit`; `isBackedUp` stays as the raw fact only the agent can compute), in `packages/shared/src/agent.ts`. The RESPONSE schemas are unchanged, and parsing strips a derived field an old agent still sends. The backend derives them at ingest in `telemetry/services/agentDerive.ts` with the local path's own rules (`classifyPowerCircuit`, `classifyBuilding`, the unit catalog), joining each machine's fuse from the latest power reading if it is not stale (three power intervals), else leaving `state` out. A parity test proves that the local path's output, stripped and re-derived, is identical. The agent-input parts also carry INPUT BOUNDS (architect, before any agent ships): strings at most 200 characters (the history CHECKs), at most 20,000 buildings, 1,000 circuits, 16 rates per building and 256 players, finite numbers, MW/rates/clock speed at least 0 and battery percent 0 to 100; a body that breaks one is refused (400), never clamped. Fixtures, tests and the runbook `agents.md` updated. No agent is deployed yet, so nothing changes for a live server.
